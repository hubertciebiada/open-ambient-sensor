# Shared ESPHome YAML fragments (packages) will be placed here:
#   - sensors.yaml       (SEN66, LD2410)
#   - status-led.yaml    (WS2812 breathing effect + AQI-to-colour mapping)
#   - ble-proxy.yaml     (Bluetooth proxy)
#
# Status: TODO — extract from `oas.yaml` once the base configuration is validated.
